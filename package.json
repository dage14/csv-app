{
  "name": "csv-app",
  "version": "1.0.0",
  "scripts": {
    "start:server": "npm start --prefix server",
    "start:client": "npm start --prefix csv-client",
    "dev": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "install-all": "npm install --prefix server && npm install --prefix csv-client"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
